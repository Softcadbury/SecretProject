(function (angular) {
    'use strict';

    angular
        .module('app')
        .controller('confirmationController', ['$rootScope', '$scope', confirmationController]);

    function confirmationController($rootScope, $scope) {
    }
})(angular);