<div id="chat">
    <h1 class="page-title">@Resource.Application_ChatRooms</h1>
    <div class="row">
        <div class="col-sm-4">
            <div class="list-group">
                <div class="list-group-item" ng-repeat="chatRoom in chatRooms" ng-class="{ active: isActive(chatRoom)}" ng-click="changeRoom(chatRoom)">
                    {{ chatRoom.Title }}
                    <span class="badge">14</span>
                </div>
            </div>
        </div>
        <div class="col-sm-8">
            <div class="chat-area">
                <div ng-repeat="message in messages">
                    <p>{{ message.user }} : {{ message.message }}</p>
                </div>
            </div>
            <form role="form">
                <div class="input-group">
                    <input type="text" class="form-control" ng-model="newMessage">
                    <span class="input-group-btn">
                        <button class="btn btn-default" type="submit" ng-click="send()">Send</button>
                    </span>
                </div>
            </form>
        </div>
    </div>
</div>